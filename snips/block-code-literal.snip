

> Block code literals are not compatible with\
  the snip processing so escape them.  Delete\
  the `bas`es to see what the highlighter does.

\```
\(def Y (fn [f]
\       ((fn [x]
\          (x x))
\        (fn [x]
\          (f (fn [y]
\               ((x x) y)))))))
\```

